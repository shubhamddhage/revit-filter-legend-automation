<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Create Filters"
    Height="520"
    Width="540"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Background="#F5F7FA">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="View Filter Generator"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#1F2937"/>
                <TextBlock Text="Create one color-coded filter for each unique parameter value in the active view."
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           Foreground="#4B5563"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Category" FontWeight="SemiBold" Foreground="#374151"/>
                <ComboBox x:Name="CategoryCombo"
                          Margin="0,8,0,0"
                          Height="32"
                          SelectionChanged="category_changed"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Parameter" FontWeight="SemiBold" Foreground="#374151"/>
                <ComboBox x:Name="ParameterCombo"
                          Margin="0,8,0,0"
                          Height="32"
                          SelectionChanged="parameter_changed"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="White" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Detected filter values"
                           FontWeight="SemiBold"
                           Foreground="#374151"/>
                <ListBox x:Name="ValueList"
                         Margin="0,10,0,0"
                         BorderThickness="1"
                         BorderBrush="#D1D5DB"
                         Height="180"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="4" Background="#EEF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,0,12">
            <TextBlock x:Name="StatusText"
                       Text="Ready"
                       Foreground="#1D4ED8"
                       TextWrapping="Wrap"/>
        </Border>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="110"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="Generate"
                    Height="36"
                    Background="#0F766E"
                    Foreground="White"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Click="generate_filters"/>

            <Button Grid.Column="3"
                    Content="Close"
                    Height="36"
                    Background="#E5E7EB"
                    Foreground="#111827"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Click="close_window"/>
        </Grid>
    </Grid>
</Window>